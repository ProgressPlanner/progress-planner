---
phase: 01-plugin-status-endpoint
plan: 01
type: execute
depends_on: []
files_modified: [classes/rest/class-remote-status.php]
---

<objective>
Complete the Remote_Status REST controller and fix missing response fields.

Purpose: The `GET /remote/status` endpoint is already implemented on the feature branch but is missing `php_version` in the site info response, which the spec requires. This plan adds the missing field and verifies the controller is correct.
Output: A fully spec-compliant Remote_Status controller ready for static analysis and commit.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

**Key files:**
@classes/rest/class-remote-status.php
@classes/rest/class-base.php
@classes/class-base.php

**Constraining decisions:**
- Bearer token in Authorization header (not URL path) — per spec
- HMAC signature using Bearer token as shared secret — per spec
- 5-minute timestamp tolerance for replay protection — per spec
- Response must include: site info (WP version, PHP version, PP version), recommendations with metadata, stats

**Current state:**
- `class-remote-status.php` exists as untracked file with full implementation
- `class-base.php` is modified with `get_rest__remote_status()` method tag and init call
- Missing: `php_version` field in the site info response object (line ~172)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add php_version to site info response</name>
  <files>classes/rest/class-remote-status.php</files>
  <action>In the `get_status()` method, add `'php_version' => \phpversion(),` to the `'site'` array in the response, after the `'wp_version'` line. This completes the spec requirement for "site info (WP version, PHP version, plugin version)". Use the native `\phpversion()` function with leading backslash per project conventions.</action>
  <verify>Read the file and confirm the site array contains name, wp_version, php_version, and pp_version</verify>
  <done>The site info object in the response includes all four fields: name, wp_version, php_version, pp_version</done>
</task>

<task type="auto">
  <name>Task 2: Run static analysis and fix any issues</name>
  <files>classes/rest/class-remote-status.php, classes/class-base.php</files>
  <action>Run PHPCS (`composer check-cs`), PHPStan (`composer phpstan` or equivalent), and PHP-CS-Fixer (`composer fix-cs` if available) on the modified files. Fix any violations found. Common issues to watch for: missing PHPDoc for the new field, native function backslash prefix, WordPress-Docs compliance, PHPStan level 10 type safety.</action>
  <verify>`composer check-cs` passes with no errors. PHPStan passes with no errors on the modified files.</verify>
  <done>All static analysis tools pass cleanly on both modified files</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `php_version` field present in site info response
- [ ] `composer check-cs` passes (or equivalent PHPCS command)
- [ ] PHPStan passes at level 10
- [ ] No new errors or warnings introduced
</verification>

<success_criteria>

- Remote_Status response schema matches spec (site info with WP version, PHP version, PP version)
- All static analysis tools pass
- Code follows project conventions (tabs, leading backslash, WordPress-Docs)
</success_criteria>

<output>
After completion, create `.planning/phases/01-plugin-status-endpoint/01-01-SUMMARY.md`
</output>
