---
phase: 01-plugin-status-endpoint
plan: 02
type: execute
depends_on: ["01-01"]
files_modified: [classes/rest/class-remote-status.php, classes/class-base.php]
---

<objective>
Commit the Remote_Status endpoint and verify it works end-to-end.

Purpose: The endpoint code is complete and lint-clean after Plan 01. This plan commits the work, verifies the endpoint responds correctly via a local request, and completes Phase 1.
Output: Committed, working `/remote/status` endpoint on the feature branch.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-plugin-status-endpoint/01-01-SUMMARY.md

**Key files:**
@classes/rest/class-remote-status.php
@classes/class-base.php

**Current state:**
- After Plan 01, both files should be lint-clean and spec-compliant
- `class-remote-status.php` is untracked (new file)
- `class-base.php` is modified (added method tag + init call)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Commit the Remote_Status endpoint</name>
  <files>classes/rest/class-remote-status.php, classes/class-base.php</files>
  <action>Stage both files and create a commit with message: `feat(remote-api): add GET /remote/status endpoint`. The commit message body should mention: read-only endpoint returning site info (WP/PHP/PP versions), active recommendations with metadata, and stats (pending/snoozed/completed_today). Include Bearer token + HMAC signature verification with 5-minute replay protection.</action>
  <verify>`git log -1 --oneline` shows the new commit. `git status` shows clean working tree for these files.</verify>
  <done>Both files committed on feature/remote-api-phase1 branch</done>
</task>

<task type="auto">
  <name>Task 2: Verify endpoint responds correctly</name>
  <files>classes/rest/class-remote-status.php</files>
  <action>Make a local HTTP request to verify the endpoint is registered and returns the expected response shape. Use `wp rest endpoint list --path=/progress-planner/v1/remote/status` via WP-CLI to confirm the endpoint exists. If WP-CLI is available, also run `wp eval "echo json_encode(rest_get_server()->get_routes());"` and grep for remote/status. Note: A full authenticated request test is not needed here since auth is Phase 2's concern — just confirm the route is registered and the controller loads without fatal errors.</action>
  <verify>WP-CLI confirms the `/progress-planner/v1/remote/status` route is registered with GET method</verify>
  <done>Endpoint is registered, route exists in the REST API, no PHP fatal errors on load</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] Commit exists on feature branch with both files
- [ ] REST route `/progress-planner/v1/remote/status` is registered
- [ ] No PHP fatal errors when loading the plugin
- [ ] Response schema includes site (name, wp_version, php_version, pp_version), recommendations array, stats object
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Phase 1 complete: working read-only `/remote/status` endpoint committed to feature branch
</success_criteria>

<output>
After completion, create `.planning/phases/01-plugin-status-endpoint/01-02-SUMMARY.md`
</output>
